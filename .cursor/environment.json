{
  "name": "Snowflake Dry Run",
  "install": "python3 -m venv .venv\n.venv/bin/pip install -U pip\n.venv/bin/pip install -e \".[dev]\"",
  "start": ".venv/bin/uvicorn snowflake_dryrun.app:app --host 0.0.0.0 --port 48731"
}
